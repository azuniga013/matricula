# Pendientes del proyecto

Registro operativo canónico de trabajo pendiente. Se actualiza junto con cada
tarea; los documentos fechados, `docs/avance.md` y el historial de
`docs/spec-feed-planCobro.md` son antecedentes, no la lista vigente.

## Cómo mantenerlo

- Estados permitidos: `pendiente`, `en_progreso`, `por_validar`, `bloqueado`,
  `completado` y `descartado`.
- Cada tarea debe tener un ID estable. No reutilizar IDs ni borrar tareas
  históricas; cambiar su estado y añadir una nota breve.
- Antes de comenzar una tarea, pasarla a `en_progreso`; al terminar, registrar
  evidencia (prueba, comando, pantalla o decisión) y dejarla en
  `completado`, `por_validar` o `bloqueado`.
- Si una tarea resulta obsoleta por la arquitectura actual, marcarla
  `descartado` explicando el motivo, en vez de conservarla como pendiente.
- Revisar este archivo antes de iniciar trabajo y actualizarlo antes de cerrar
  la tarea.
- Cuando haya trabajo en paralelo con varios agentes, la actualización de este
  archivo y de `docs/COORDINACION_AGENTES.md` es obligatoria para cualquier
  cambio relevante, prueba nueva o ajuste de criterio.

## Estado al 2026-08-05

> Regla de arquitectura (2026-08-10): la lógica nueva de negocio no entra en
> los controladores. Los casos de uso viven en `app/Modules/<Modulo>/CasosUso`,
> el acceso a datos en `app/Modules/<Modulo>/Repositorios` y los contratos de
> respuesta en `app/Modules/<Modulo>/ResultadoCasoUso`. Los controladores solo
> orquestan HTTP y delegan.
>
> Guía de decisión (2026-08-10): la meta es un monolito Laravel modular con
> principios de hexagonal, no una hexagonal pura. Extraer casos de uso solo
> donde haya reglas de negocio; dejar el CRUD simple y las lecturas en el
> controlador. Criterios detallados en `P-032`, `P-033` y `P-034`.

### Pendientes prioritarios

| ID | Estado | Tarea | Evidencia o siguiente paso |
|---|---|---|---|
| `P-001` | `en_progreso` | Completar el filtro académico reutilizable en gestiones de matrícula, recibos, cierres de caja, inventario y ficha/consultas de estudiante. | Revisión técnica 2026-08-21: Gestiones de matrícula y Recibos de Caja aplican `período -> plan -> nivel -> grupo/oferta` en interfaz y API. Inventario y cierres de caja operan por sucursal/stock/fecha y no dependen de oferta; la ficha del estudiante conserva el historial completo sin recortarlo por filtro académico. Ajuste adicional 2026-08-21: las pantallas con filtro inicial ahora cargan automáticamente la información correspondiente al entrar, sin requerir pulsar `Actualizar`, en `resources/views/admin/ofertas.blade.php`, `resources/views/admin/matriculas.blade.php`, `resources/views/admin/pagos.blade.php`, `resources/views/admin/asistencias.blade.php`, `resources/views/admin/calificaciones.blade.php` y `resources/views/admin/reportes.blade.php`. Corrección adicional 2026-08-21: `resources/views/admin/asistencias.blade.php` ahora ignora respuestas asíncronas fuera de orden al cambiar período/sucursal/nivel/grupo, evitando que un resultado viejo reemplace el filtro más reciente. Ajuste adicional 2026-08-21 en Inventario/Kardex: `resources/views/admin/inventario.blade.php` y `app/Http/Controllers/Api/V1/Inventario/InventarioLibroController.php` ahora permiten filtrar movimientos por `fecha_desde`. Verificado con `php artisan test tests/Feature/InventarioLibroTest.php`, `php artisan view:cache` y `npm run build`. Pendiente: aceptación visual/manual para cerrar. |
| `P-002` | `completado` | Completar pruebas automatizadas de matrícula, pagos, asistencia, calificaciones, monitor de cupos, reportes y flujos de matrícula. | **Prueba de integración transversal añadida:** `tests/Feature/IntegracionFlujoAcademicoTest.php` recorre el flujo de negocio completo por los casos de uso ya modularizados (P-029 Matrículas, P-028 Pagos, P-035 Caja/Calificaciones): reservar → confirmar (genera obligaciones) → abrir sesión de caja → registrar pago de matrícula (genera recibo, aplica obligación, matricula al estudiante, asigna sesión de caja) → registrar calificación (sincroniza historial académico) → corregir calificación (marca `corregido` y `reprobado` el historial) → anular recibo → cerrar sesión de caja (genera detalle de cierre). Además valida consistencia de cupos y obligaciones tras el pago. **Denegación RBAC y alcance añadidos** en `tests/Feature/AlcanceApiTest.php` (6 pruebas, 15 aserciones): usuario global ve todas las sucursales, usuario con sucursal asignada solo ve la suya, usuario sin alcance no ve nada, docente solo ve sus ofertas, y denegación 403 para `ofertas/monitor` y `asistencias/ofertas-disponibles`. **Denegación RBAC por módulo ampliada** en `tests/Feature/DenegacionRbacTest.php` (**15 pruebas, 31 aserciones**): Seguridad, Gestiones de Matrícula, Estudiantes, Matrículas, Pagos, Recibos de Caja, Caja, Cierre de Caja, Calificaciones, Inventario y Reportes. **Cobertura adicional de P-003 añadida en pruebas:** `CalificacionTest` cubre alcance docente en listado y detalle; `PortalEstudianteTest` cubre alcance de alumno/propietario para no exponer matrículas, pagos, recibos, historial académico ni permitir eliminar o registrar pagos sobre recursos de otro estudiante; `AlcanceAdministrativoApiTest.php` (**10 pruebas, 44 aserciones**) y `AlcanceAdministrativoTest.php` (**9 pruebas, 23 aserciones**) agregan alcance administrativo por sucursal/propietario sobre `Estudiantes`, `Matrículas`, `Pagos`, `Recibos`, `Caja`, `Inventario` y los reportes `financieros/pagos-pendientes`, `academicos/grupo` y `recibos/por-orden`. Verificado con suite completa final: **309 pruebas, 1039 aserciones**. |
| `P-003` | `completado` | Cerrar la cobertura RBAC por entidad y alcance. | **Cobertura acumulada por tipo de alcance:** `CalificacionTest` cubre alcance docente en listado y detalle; `PortalEstudianteTest` cubre alcance alumno/propietario en matrículas, pagos, recibos, historial académico, eliminación de pagos ajenos y registro de pagos sobre matrícula ajena; `AlcanceAdministrativoApiTest.php` cubre alcance administrativo por sucursal/propietario en `Estudiantes`, `Matrículas`, `Pagos`, `Recibos`, `Caja`, `Inventario` y reportes (`academicos/grupo`, `recibos/por-orden`, `financieros/pagos-pendientes`). Para soportarlo se aplicó alcance backend en `EstudianteController`, `MatriculaController`, `PagoController`, `ReciboCajaController`, `SesionCajaController`, `InventarioLibroController` y `ReporteController`, y `ResolutorAlcanceDatos` ahora reconoce `estudiantes` como entidad con sucursal. La cobertura de denegación RBAC por módulo sigue documentada en `P-002` mediante `DenegacionRbacTest.php`. Verificado con `AlcanceAdministrativoApiTest`, `EstudianteTest`, `PagoTest`, `MatriculaTest`, `CajaTest`, `InventarioLibroTest` y `ReporteTest`. |
| `P-004` | `completado` | Revisar seguridad de sesiones administrativas. | Se añadió `app/Http/Middleware/ValidarSesionAdministrativa.php` y el grupo `/api/v1` administrativo ahora pasa por `admin.session` antes de `auth:sanctum`: si el `token_hash` aparece revocado en `sesiones_usuario`, responde `401_SESION_REVOCADA`; además crea/actualiza el registro interno de sesión y `ultimo_acceso` para tokens válidos. `AuthController` ahora usa `config/seguridad.php` para el bloqueo (`5` intentos, ventana `15`, bloqueo `15`), responde `423_USUARIO_BLOQUEADO` al superar el límite y aplica la misma lógica al login web. `UsuarioController` revoca sesiones y tokens al inactivar/restablecer contraseña, registra auditoría (`inactivar_usuario`, `restablecer_contrasena_usuario`) y protege al último `SUPERADMIN` activo tanto al inactivar como al retirar el rol. `SesionController` registra `revocar_sesion` en bitácora. Verificado con `SeguridadSesionesAdministrativasTest` (7 pruebas, 41 aserciones), `AuthTest` (7 pruebas), `UsuarioTest` (3 pruebas) y `AuditoriaTest` (2 pruebas): 19 pruebas, 82 aserciones en verde. |
| `P-005` | `pendiente` | Registrar aceptación funcional de monitor de cupos, PDF y Excel en escritorio y móvil. | Ejecutar `docs/PRUEBAS_ACEPTACION_RESPONSIVE.md` y adjuntar resultado o incidencia. |
| `P-006` | `completado` | Validar despliegue en producción. | Despliegue por `git push mojahost main` completado; validación funcional en producción confirmada por el usuario el 2026-08-05. |

### Pendientes funcionales

| ID | Estado | Tarea | Evidencia o siguiente paso |
|---|---|---|---|
| `P-007` | `completado` | Probar integralmente las configuraciones de flujo de matrícula. | Nueva suite `tests/Feature/FlujoMatriculaConfiguracionTest.php` (9 pruebas, 32 aserciones) cubre: resolución por origen/concepto/método, precedencia por `id`, fallback `tecnico` para `portal_administrativo`, desactivación que conserva asociaciones y deja de resolver la configuración, y banderas críticas en backend (`habilita_reserva_cupo`, `habilita_solicitud_link`, `habilita_carga_comprobante`, `requiere_comprobante`, `habilita_aprobacion_pago`, `habilita_reenganche`, `habilita_whatsapp`). Se corrigió backend para respetar `habilita_aprobacion_pago` en el pago administrativo directo, `habilita_reenganche` en `PortalEstudianteController@reengancharFlujoPago` y `habilita_whatsapp` en `PortalEstudianteController@whatsapp`. Verificado además con `PagoTest` (30 pruebas, 120 aserciones) y `PortalEstudianteTest --filter "reservar_matricula|registrar_pago_link_sin_comprobante_queda_en_solicita_link|no_puede_solicitar_link_doble_para_misma_obligacion|reenganchar_flujo_pago_reencauza_matricula_y_pago|whatsapp_sin_pago|whatsapp_link_invalido"` (6 pruebas, 27 aserciones). |
| `P-008` | `completado` | Revisar la consistencia de las transiciones de matrícula, pago, obligación, cupo y recibo. | Se corrigieron dos divergencias de backend y dos documentales. **Backend:** `RegistrarPago` y `PortalEstudianteController@registrarPago` ahora rechazan `solicitar_link` cuando el método no permite link (`422_METODO_NO_PERMITE_LINK`); `RegistrarPago` y `PagoTarjetaController` rechazan pagar `CUO` dejando fuera una obligación `MAT` pendiente (`422_MATRICULA_OBLIGATORIA`). Para centralizar la regla se creó `app/Services/Pagos/ValidadorReglasPago.php`. **Documentación:** `docs/REGLAS_NEGOCIO_POR_DOMINIO.md` ahora distingue el registro administrativo directo (pago aprobado inmediato cuando el flujo lo permite) de la aprobación posterior de pagos `pendiente`/`en_revision`, y ajusta la regla de reimpresión de recibos al comportamiento actual (contador + usuario/fecha, sin motivo persistido). Verificado con `PagoConsistenciaTransicionesTest` (3 pruebas, 9 aserciones), `PagoTest` (30 pruebas, 120 aserciones) y `PortalEstudianteTest --filter "registrar_pago_link_sin_comprobante_queda_en_solicita_link|no_puede_solicitar_link_doble_para_misma_obligacion|reenganchar_flujo_pago_reencauza_matricula_y_pago"` (3 pruebas, 17 aserciones). |
| `P-009` | `completado` | Revisar fechas y datos históricos de pagos y recibos. | Regla funcional aclarada y aplicada: `creado_en`, `fecha_proceso` y `fecha_recibo` se tratan como la misma fecha de negocio. `Pago` y `ReciboCaja` ahora resuelven una fecha funcional única para lectura histórica; el portal del estudiante y el detalle de recibos consumen esa fecha; y la creación de pagos del portal, pagos con tarjeta y recibos nuevos persiste la misma marca temporal en los tres campos para evitar nuevas divergencias. Verificado con `PagoFechasHistoricasTest` (2 pruebas, 10 aserciones) y `PortalEstudianteTest --filter registrar_pago` (2 pruebas, 8 aserciones). |
| `P-013` | `completado` | Exigir cuenta bancaria para depósitos y transferencias. | Migración `2026_08_05_000001`; API administrativa y portal del estudiante validan cuenta activa, la persisten en el pago y la muestran para seleccionar. Verificado con `PagoTest` enfocado y `npm run build`. |
| `P-014` | `completado` | Mostrar cuentas bancarias de depósito en Dashboard y pagos del Estudiante. | Dashboard y formulario de pagos reciben las cuentas activas en la respuesta principal del portal; se listan banco, nombre, número y tipo, y se muestra una alerta visible si no hay cuentas configuradas. La carga de comprobante también recupera las cuentas cuando se abre directamente. |
| `P-015` | `completado` | Seleccionar plan de estudio antes de la oferta al matricular. | Matrícula administrativa y portal aplican Plan → Nivel → Oferta; ambas APIs validan que la oferta pertenezca al plan enviado y conservan `oferta_academica_id` como fuente de verdad. Verificado con `MatriculaTest`, `PortalEstudianteTest`, caché Blade y build. |
| `P-016` | `completado` | Documentar el proceso de cambio de plan de estudios. | `docs/PROPUESTA_CAMBIO_PLAN_ESTUDIOS.md` define flujo, controles académicos/financieros, trazabilidad, endpoints, pruebas y decisiones pendientes. |
| `P-017` | `completado` | Corregir la carga de grupos y estudiantes en Asistencias. | La vista conserva correctamente la oferta seleccionada, muestra fallos de API y el backend incorpora ofertas cerradas o llenas que continúan requiriendo pase de lista. Se corrigió la carga del régimen académico desde el nivel de la oferta, que provocaba error 500 al consultar grupos. Verificado con `CalificacionTest` enfocado (incluye regresión de asistencias), caché Blade y `npm run build`. |
| `P-018` | `completado` | Habilitar generación de certificado desde Calificaciones para docentes. | El historial se sincroniza al guardar una calificación; el botón de Calificaciones permite generar el certificado con `calificaciones.modificar`, y el Historial del Portal permite al estudiante emitir solo su certificado aprobado. Se corrigió la búsqueda del historial al emitir, evitando duplicados, y se ubicó la ruta del portal fuera de la autenticación administrativa. Verificado en `CalificacionTest`, `PortalEstudianteTest`, caché Blade y build. |
| `P-019` | `completado` | Usar verbos compatibles con IIS/SmarterASP en pantallas administrativas. | `window.api.actualizar()` centraliza las actualizaciones por `POST`; Ofertas, Estudiantes, Inventario, Catálogos, Planes de cobro, Seguridad y configuraciones de matrícula lo usan. La protección global convierte cualquier `PUT`/`PATCH` accidental para `/api/v1` en `POST`; las rutas conservan compatibilidad de backend. Auditoría: Matrícula, Pagos y Recibos ya operaban por `POST`. Verificado con `OfertaAcademicaTest`, `MatriculaTest`, `PagoTest`, caché Blade y build. |
| `P-020` | `completado` | Corregir acceso y configuración de proveedores de pago. | La pantalla usa el token administrativo correcto y conserva la sesión; el API enmascara configuraciones sensibles y acepta el contrato `config` de la pantalla. Los permisos `configuracion.pagos.consultar` y `configuracion.pagos.modificar` están incluidos para `SUPERADMIN` y `ADMIN_GENERAL` mediante `SeguridadRbacSeeder`. Verificado con `ProveedorPagoTest`, caché Blade y build. |
| `P-021` | `en_progreso` | Crear APK offline para docentes. | Base Expo en `mobile-docentes/`: login administrativo de docente, SecureStore, SQLite, ofertas, alumnos, asistencia, notas y cola offline. Incluye menú de Ofertas Académicas, Asistencia Diaria y Calificaciones: cada módulo filtra por período, muestra ofertas del docente y después su lista de estudiantes. El Android valida por Wi-Fi el certificado Let’s Encrypt y login/`/me` entregan `docente_id`. Se corrigió la asignación de permisos mínimos del rol `DOCENTE` y los mensajes de sincronización para no ocultar rechazos del servidor. Se corrigió `build.gradle` para usar `signingConfigs.release` (firma institucional). Se desinstaló v0.1.0 (firma anterior perdida), se recreó el keystore institucional con contraseña segura ASCII, se compiló y firmó v0.1.1 con `apksigner` v2/v3 y se instaló exitosamente en el Samsung S24+ de prueba. `keystore.properties` actualizado. 2026-08-07: el hosting bloquea `POST` multipart de 61 MB por `client_max_body_size`/HTTP2 (`ERR_HTTP2_PROTOCOL_ERROR`); se añadió la modalidad **registrar desde APK ya colocado** en `storage/app/private/apk-docentes/` (raíz del disco `local` en Laravel 12, sin upload HTTP): `POST /distribucion-apk/docentes?desde_servidor=1` lee el archivo más reciente del servidor, calcula hash/tamaño y lo publica; el panel tiene checkbox "Registrar desde un APK ya colocado". Verificado con `DistribucionApkDocenteTest` (5 pruebas, luego 22 aserciones al añadir cobertura de descarga) y build. 2026-08-07: se corrigió el `500` de `/apk/docentes/descargar`: `Storage::disk('local')->download()` devuelve `StreamedResponse` en Laravel 12 y el return type solo admitía `BinaryFileResponse|Response`; se amplió el contrato y se verificó la descarga. Desplegado y **verificado en producción**: la descarga responde `200` con `application/vnd.android.package-archive` (63 939 375 bytes ≈ 60,98 MB); la URL pública muestra "Versión 0.1.0". Pendiente: confirmar que se distribuya la v0.1.1 (el registro activo es v0.1.0; la v0.1.1 quedó de borrador) y publicarla desde el panel, y probar login con credenciales docentes reales en el dispositivo. La llave y APK están ignoradas; el panel administra publicaciones y la URL pública `/apk/docentes` entrega solo la versión activa. **2026-08-07/08**: flujo de asistencia renovado en `App.jsx` (v0.1.2 / versionCode 3, SHA-256 `7923cf5d…`): al entrar muestra fecha y lista de estudiantes; al tocar un estudiante se abre una vista con solo su nombre y las **banderas** de estado (Presente/Falta/Justificada/Tardanza); desde ahí se marca y al volver la lista muestra solo el estado elegido con un badge de color. Compilada y firmada en `mobile-docentes/android` con Gradle `assembleRelease` (firma institucional), registrada y publicada en producción vía API con token Sanctum de deploy (id 10; despublicó la v0.1.1). **Verificado en producción**: `GET /apk/docentes/descargar` responde `200` con `application/vnd.android.package-archive` (63 889 088 bytes) y el panel lista la v0.1.2 publicada. **2026-08-08**: se corrigió el retorno de asistencia tras refresco: la app nunca descargaba los estados ya guardados (`/asistencias/por-oferta`), siempre reinicializaba todo en `presente`; ahora `openOffer`/`refresh` cargan los estados del servidor (y caché local por oferta+fecha) y el guardado persiste el caché. Además: ofertas sin matrículas ya no fallan (`registrar` con lista vacía responde `data.registradas=0` en vez de `422`); la app bloquea guardar sin estudiantes; y ante `401` la sesión cierra y pide reingresar en vez de reintentar el loop. Publicada **v0.1.3 / versionCode 4** en producción (id 11, SHA-256 `3f4b324c…`): `GET /apk/docentes/descargar` responde `200` con `63 913 348` bytes. `CalificacionTest` con 3 pruebas de asistencia en verde (incluida lista vacía). 2026-08-07: se compiló y firmó **v0.1.4 / versionCode 5** (id 12) con: login rediseñado (card, branding, botón primario) y **acceso biométrico** (expo-local-authentication, credenciales guardadas en SecureStore, reingreso con huella/rostro) y fecha de asistencia seleccionable por **calendario nativo** que refresca los estados del día desde el servidor/caché al cambiar. Publicado en producción como versión activa; `GET /apk/docentes/descargar` responde `200` con `66 615 634` bytes y SHA-256 `32dbd00c7ee8d5df5cdedd49acc5559a39df211c69addde0045fe5da9bb63ef1` (idéntico al compilado).  2026-08-07: se corrigió la **biometría** (el guardado no sobrevivía al cierre de sesión): `closeSession` ya no borra las credenciales, el login ahora tiene un interruptor "Acceso con huella" que guarda correo+contraseña en SecureStore al iniciar sesión y lo limpia al desactivarse; el botón "Entrar con huella o rostro" persiste tras cerrar sesión. Además el **login se amplió** (card más grande, logo y tipografía mayores) y se añadió **Ver/Ocultar contraseña** para validar lo escrito. Compilado y publicado como **v0.1.5 / versionCode 6** (id 13, SHA-256 `62b2f08e38100e5af926cc5cc355cb46d23e8713a396d54924118fae4eb2341a`); `GET /apk/docentes/descargar` responde `200` con `66 617 606` bytes idéntico al compilado. |
| `P-022` | `en_progreso` | Notificar a responsables por falta o tardanza. | **Base backend implementada y canal email operativo:** nuevas tablas `contactos_responsable_estudiante` y `notificaciones_asistencia`; modelos `ContactoResponsableEstudiante`/`NotificacionAsistencia`; evento `AsistenciaNotificableRegistrada`; listener en cola `CrearNotificacionesAsistencia` que crea registros `pendiente` por contacto/canal consentido con clave idempotente `asistencia:{id}:contacto:{id}:canal:{canal}:estado:{estado}`. `AsistenciaController@registrar` ahora emite el evento después del commit cuando el estado final es `falta` o `tardanza`, y se corrigió el reintento de asistencia para respetar la unicidad `matricula + fecha`. **CRUD y UI administrativa inicial listos:** `ContactoResponsableEstudianteController` y rutas anidadas bajo `estudiantes` permiten listar, crear, actualizar y desactivar contactos con permisos `estudiantes.consultar`/`estudiantes.modificar`, validando consentimiento antes de activar canales y respetando el alcance de sucursal del estudiante; la ficha de `admin/estudiantes` ya expone la pestaña `Responsables` con modal CRUD. **Procesamiento explícito añadido:** `config/notificaciones_asistencia.php`, servicio `ProcesadorNotificacionAsistencia`, servicio `ConfiguracionNotificacionesAsistencia`, job `ProcesarNotificacionAsistencia` y comando `asistencias:notificaciones-procesar`; cuando los envíos reales siguen deshabilitados, la notificación pasa a `omitida`; si el canal es `email` y los envíos están habilitados, se manda `NotificacionAsistenciaResponsable` con `Mail` y se registra en `bitacora_correos`; **WhatsApp ya quedó configurable** por parámetros/global config (`WHATSAPP_HABILITADO`, `WHATSAPP_DRIVER`, `WHATSAPP_REMITENTE`, `WHATSAPP_PLANTILLA`) y soporta modo `stub`, pero no proveedor oficial real todavía. El `ParametroGlobalSeeder` ya publica esos parámetros para administración desde Seguridad y `ConfiguracionNotificacionesAsistencia` los resuelve desde base de datos. Verificado con `NotificacionAsistenciaTest` (7 pruebas, 20 aserciones), `ContactoResponsableEstudianteTest` (4 pruebas, 13 aserciones), `ParametroGlobalTest` (8 pruebas, 19 aserciones), `view:cache` y regresión de asistencias en `CalificacionTest --filter "registrar_asistencia_de_la_oferta|registrar_asistencia_con_lista_vacia_no_falla|por_oferta_devuelve_asistencias_sincronizadas_de_la_fecha|faltas_por_oferta_agrupa_faltas_del_periodo"` (4 pruebas, 18 aserciones). Pendiente: proveedor oficial de WhatsApp y activación/credenciales de correo en producción con protección de datos. |
| `P-023` | `por_validar` | Crear usuarios docentes y asignar roles desde Seguridad. | El formulario envía confirmación de contraseña, uno o más roles y vínculo con docente existente; `UsuarioTest` pasó (3 pruebas, 9 aserciones) y el build Blade completó. Falta aceptación visual en producción. |
| `P-024` | `completado` | Mostrar el último acceso de los usuarios en Seguridad. | El listado de usuarios no devolvía `ultimo_acceso` (campo vacío): ese dato solo vive en `sesiones_usuario` y el `User` no lo incluye. `UsuarioController@index` ahora lo expone como subconsulta `MAX(ultimo_acceso)` por usuario y la tabla lo formatea con `formatearFecha`. Verificado con `UsuarioTest` ampliado (11 aserciones). |
| `P-025` | `completado` | Cargar el catálogo de permisos al abrir "Administrar Permisos" de un rol. | La modal se alimentaba de `todosPermisos`, que solo se llenaba al visitar la pestaña Permisos; al entrar directo a Roles la matriz salía vacía. `editRolePermisos` ahora consulta en paralelo `/seguridad/permisos` y `/roles/{id}/permisos` y agrupa por módulo dentro del modal. Verificado con `php artisan view:cache`. |
| `P-026` | `completado` | Cargar las asistencias ya registradas al seleccionar fecha en Asistencias. | `cargarEstudiantes` fusionaba solo el listado y dejaba todo en `presente`; ahora consulta `/asistencias/por-oferta` para la fecha elegida y pinta el último estado sincronizado. Además se corrigió `porOferta` (usaba `where('fecha')`; la fecha se guarda con hora y no devolvía nada en SQLite) pasando a `whereDate('fecha')`. Verificado con `CalificacionTest` (`por_oferta_devuelve_asistencias_sincronizadas_de_la_fecha`), `view:cache` y suite completa. |
| `P-027` | `completado` | Llevar faltas automáticas desde asistencias diarias en Ingreso de Calificaciones. | El registro de notas pedía faltas a mano aunque ya estaban en asistencias. `cambioGrupo` ahora consulta `/asistencias/faltas-por-oferta` y carga, filtradas por el rango de fechas del período de la oferta (cuenta `cuenta_como_falta=true`, excluye fechas fuera del periodo), las faltas por estudiante; prevalece sobre el valor manual si existe asistencia, si no usa la nota guardada y 0. Nuevo endpoint `GET /asistencias/faltas-por-oferta` (permiso `asistencias.consultar`) con `LEFT JOIN` y `whereDate`. Verificado con `CalificacionTest` (`faltas_por_oferta_agrupa_faltas_del_periodo`), `view:cache` y suite completa. |
| `P-028` | `completado` | Extraer la lógica de negocio del módulo Pagos a casos de uso y repositorios en `app/Modules/Pagos`. | Extraídos a `CasosUso/`: `AprobarPago`, `RegistrarPago`, `RechazarPago`, `ActualizarLinkPago`, `SubirComprobantePago` y `EliminarPagoTotal`; junto con `ContextoUsuario`, `ResultadoCasoUso`, `PagoRepositorio`/`EloquentPagoRepositorio` y `GeneradorReciboCaja`. `PagoController` bajó de 731 a 356 líneas y solo orquesta HTTP; se eliminaron los métodos privados de recibo y de cuenta bancaria. El repositorio se vincula en `AppServiceProvider`. Verificado con `PagoTest` (29 pruebas, 6 nuevas de casos de uso) y suite completa (211 pruebas, 680 aserciones). |
| `P-037` | `completado` | Corregir reenvío de credenciales del portal del estudiante que enviaba una clave no utilizable. | En `EstudianteAuthController@reenviarCredenciales`, guardar la contraseña con `update(['password' => ...])` sobre `AccesoEstudiante` dejaba un hash que luego no validaba en login; se cambió a asignación directa + `save()`. `EstudianteTest::test_reenviar_credenciales_estudiante` ahora extrae la clave reenviada desde `bitacora_correos`, valida `Hash::check` contra `accesos_estudiante.password` y confirma login exitoso con esa misma clave. Verificado con `php artisan test tests/Feature/EstudianteTest.php` y `php artisan view:cache`. |
| `P-038` | `completado` | Alinear el flujo de pagos con link con la regla de negocio al cargar el enlace. | Se corrigió la transición para que cargar `link_pago_url` no envíe el pago directamente a revisión: `EloquentPagoRepositorio::actualizarLink()` ahora lo mueve a `esperando_respuesta` y conserva `link_pago_estado='enviado'`. Solo cuando el estudiante confirma desde el portal (`PortalEstudianteController@confirmarLinkPago`) el pago pasa a `en_revision`. `PagoController@index` agrupa `solicita_link` + `esperando_respuesta` en la pestaña administrativa de solicitudes de link, y `resources/views/estudiante/pagos.blade.php` muestra el botón “Ya completé el pago” solo en ese estado intermedio. Verificado con `php artisan test tests/Feature/PagoTest.php --filter "solicita_link|confirmar_link_pago_mueve_a_revision|listar_solicitudes_de_link"`, `php artisan test tests/Feature/PortalEstudianteTest.php --filter "registrar_pago_link_sin_comprobante_queda_en_solicita_link|no_puede_solicitar_link_doble_para_misma_obligacion|reenganchar_flujo_pago_reencauza_matricula_y_pago"` y `php artisan view:cache`. |
| `P-039` | `completado` | Deshabilitar en producción la eliminación de pagos desde el portal del estudiante. | `resources/views/estudiante/pagos.blade.php` ahora oculta el botón `Eliminar` cuando `APP_ENV=production`, y `PortalEstudianteController@eliminarPago` responde `403_ACCION_NO_DISPONIBLE` si se intenta invocar en producción. Verificado con `php artisan view:cache` y `php artisan test tests/Feature/PortalEstudianteTest.php --filter eliminar`. |
| `P-040` | `completado` | Evitar que una matrícula activa sin aprobación académica habilite el siguiente nivel. | `ValidadorPrerrequisitos` ya no trata como prerrequisito cumplido una matrícula `reservada`, `en_revision` o `matriculado`; ahora solo cuentan historial académico aprobado, calificación aprobada real o evaluación de nivelación aprobada. Además, cuando el prerrequisito se aprobó por cursar el nivel, se exige cierre administrativo: la matrícula previa debe quedar sin obligaciones `pendiente`/`parcial` antes de permitir la siguiente reserva. Cobertura añadida: `MatriculaTest::test_no_reserva_siguiente_nivel_solo_por_tener_matricula_previa_sin_aprobacion_academica` y `MatriculaTest::test_no_reserva_siguiente_nivel_si_prerrequisito_esta_aprobado_pero_no_pagado`. Verificado con `php artisan test tests/Feature/MatriculaTest.php` y `php artisan view:cache`. |
| `P-041` | `completado` | Hacer obligatorio el plan de cobro en ofertas y bloquear reservas sin obligaciones configurables. | `OfertaAcademicaController` ahora exige `plan_cobro_id` al crear ofertas y lo mantiene obligatorio al actualizar; `ReservarMatricula` valida antes de crear la reserva que la oferta tenga un `plan_cobro` activo con detalles activos; y `GeneradorObligacionesMatricula` genera obligaciones solo desde esos detalles activos. Además, `PlanCobroSeeder` quedó idempotente y reasigna `plan_cobro_id` a las ofertas existentes según el plan (`ofertas_sin_plan=0` tras ejecutar el seeder corregido). Cobertura añadida: `OfertaAcademicaTest::test_crear_oferta_academica_requiere_plan_cobro` y `MatriculaTest::test_no_reservar_si_oferta_no_tiene_plan_de_cobro_activo_con_detalles`. Verificado con `php artisan test tests/Feature/OfertaAcademicaTest.php`, `php artisan test tests/Feature/MatriculaTest.php`, `php artisan tinker` para reasignación y `php artisan view:cache`. |
| `P-042` | `completado` | Alinear la reserva del portal del estudiante con las reglas nuevas de prerrequisitos y plan de cobro. | `PortalEstudianteController@reservarMatricula` dejó de usar la validación antigua del trait `ValidaPrerrequisitos` y ahora delega en `App\Modules\Matriculas\Servicios\ValidadorPrerrequisitos`, con la misma regla de aprobación académica + cierre administrativo del nivel previo. Además, el portal ahora bloquea la reserva si la oferta no tiene `plan_cobro` activo con detalles activos antes de crear matrícula u obligaciones. Esto corrige el caso detectado en producción donde `EST-2026-00000001` pudo reservar `A1` sin calificación de `Phonics` por una divergencia entre portal y módulo de matrículas. Verificado con `php artisan test tests/Feature/PortalEstudianteTest.php` y `php artisan view:cache`. |
| `P-043` | `completado` | Añadir soporte administrativo para monto recibido y vuelto en pagos en efectivo. | Se agregó persistencia en `pagos` para `monto_recibido` y `vuelto`, con validación en `RegistrarPago`: para método `EFE` el monto recibido es obligatorio y no puede ser menor al total. `admin/pagos` y el modal de pago desde `admin/matriculas` ahora muestran el total a pagar, capturan `monto_recibido` y calculan el vuelto. También se alineó `admin/matriculas` para cargar `cuentasBancarias` y permitir cuenta bancaria en `DEP/TRA`. Verificado con `php artisan test tests/Feature/PagoTest.php --filter "efectivo_requiere_monto_recibido_y_guarda_vuelto|registrar pago|registrar_pago_mediante_caso_de_uso|registrar_pago_con_libro_sin_existencia"` y `php artisan view:cache`. |
| `P-044` | `completado` | Exigir sesión de caja abierta para pagos administrativos aprobados sin afectar el portal del estudiante. | `RegistrarPago` y `AprobarPago` ahora rechazan pagos administrativos si el usuario no tiene una `sesiones_caja` abierta en la sucursal del pago (`422_SESION_CAJA_REQUERIDA`). El portal del estudiante no usa estos casos de uso y no se ve afectado. La lógica vive en `AplicadorEfectosPago::obtenerSesionCajaAbierta()`, reutilizada también por la asignación de `sesion_caja_id`. Verificado con `PagoTest::test_registrar_pago_administrativo_requiere_sesion_de_caja_abierta`, `php artisan test tests/Feature/PagoConsistenciaTransicionesTest.php` y `php artisan view:cache`. |
| `P-045` | `completado` | Exponer en Seguridad la asignación de múltiples sucursales por usuario. | El backend ya soportaba alcance por varias sucursales mediante `usuario_sucursales` y `UsuarioController@asignarSucursales`, pero el modal de usuarios en `admin/seguridad` no mostraba ese control. Ahora la UI carga `sucursales` del catálogo, permite seleccionarlas en creación/edición y envía la asignación al endpoint `/api/v1/seguridad/usuarios/{id}/sucursales`. Esto permite parametrizar usuarios con acceso a una, varias o ninguna sucursal globalmente según corresponda. Verificado con `php artisan view:cache`. |
| `P-046` | `completado` | Corregir el total de matriculados mostrado en el Dashboard administrativo. | `resources/views/admin/dashboard.blade.php` ya no toma el total global de `/api/v1/matriculas`; ahora consulta `/api/v1/matriculas?estado=matriculado&per_page=1` para que el contador excluya reservas y, como respaldo, filtra localmente solo elementos con `estado='matriculado'` si la respuesta no viene paginada. Esto corrige el caso reportado donde 3 matriculados + 1 reserva se mostraban como 4 matriculados. Verificado con `php artisan view:cache`. |
| `P-047` | `completado` | Consolidar documentación técnica por módulo y alcance multi-sucursal. | Se añadió `docs/RESUMEN_TECNICO_REGLAS_POR_MODULO.md` con el resumen de reglas cerradas por Matrículas, Ofertas, Pagos administrativos, Flujo de link, Portal del Estudiante y Seguridad. Además se actualizó `docs/ARQUITECTURA_RBAC.md` para aclarar que el alcance efectivo de un usuario puede ser la unión de varias sucursales asignadas en `usuario_sucursales`, y se actualizaron `docs/API_PAGOS.md` y `docs/OBLIGACIONES_PAGO_MATRICULA.md` para reflejar sesión de caja obligatoria, efectivo con `monto_recibido`/`vuelto`, flujo `solicita_link -> esperando_respuesta -> en_revision -> aprobado` y plan de cobro obligatorio. |
| `P-048` | `completado` | Separar horario lógico de WhatsApp y link operativo por período, con edición docente. | Historial cerrado: la oferta guarda `whatsapp_grupo_nombre` y `whatsapp_link_periodo`; la administración del link vive en `Mis Horarios`; el portal del estudiante muestra WhatsApp siempre que exista oferta actual. La antigua vía de catálogo fue retirada posteriormente en `P-051`. |
| `P-049` | `completado` | Mover la administración docente del link WhatsApp del período a una pantalla dedicada `Mis Horarios`. | Se creó `resources/views/admin/mis-grupos.blade.php` y la ruta `/admin/mis-grupos`, reutilizando `GET /api/v1/asistencias/ofertas-disponibles` para listar los horarios del docente con filtros de período/sucursal/nivel y `POST /api/v1/ofertas/academicas/{id}/whatsapp-periodo` para guardar el link vigente por oferta. El bloque de edición se retiró de `resources/views/admin/asistencias.blade.php`, que vuelve a enfocarse solo en pasar lista. Además, `Asistencias` ahora acepta `?oferta={id}` para abrir directamente un horario desde `Mis Horarios`. Verificado con `php artisan view:cache`. |
| `P-050` | `completado` | Simplificar la gestión de WhatsApp en ofertas y portal del estudiante. | Se añadió `ofertas_academicas.whatsapp_grupo_nombre` y se migró el nombre funcional del horario directamente en la oferta. `resources/views/admin/ofertas.blade.php` ya no pide seleccionar un grupo catálogo; ahora captura el nombre funcional y remite el link vigente a `Mis Horarios`. El portal del estudiante usa primero `whatsapp_grupo_nombre` + `whatsapp_link_periodo` de la oferta. Documentación técnica alineada en `docs/REGLAS_NEGOCIO_POR_DOMINIO.md` y `docs/RESUMEN_TECNICO_REGLAS_POR_MODULO.md`. Verificado con `php artisan migrate --force`, `php artisan test tests/Feature/PortalEstudianteTest.php --filter whatsapp`, `php artisan test tests/Feature/FlujoMatriculaConfiguracionTest.php --filter whatsapp` y `php artisan view:cache`. |
| `P-051` | `completado` | Retirar la pantalla operativa antigua de grupos de WhatsApp. | Se eliminó la ruta API, el módulo administrativo asociado y su entrada RBAC. La gestión de WhatsApp quedó centralizada en `Mis Horarios` y en los campos directos de la oferta (`whatsapp_grupo_nombre` + `whatsapp_link_periodo`). El componente UI de `admin/catalogos` ya no expone esa pestaña. Verificado con `php artisan view:cache` y migración previa de `whatsapp_grupo_nombre`. |
| `P-052` | `en_progreso` | Permitir configurar modalidades de atención habilitadas por sucursal y validar la oferta contra esa configuración. | Se agregó la relación pivote `sucursal_modalidad_atencion`, el modelo `Sucursal` expone `modalidadesAtencion()`, `SucursalController` persiste `modalidades_atencion`, `OfertaAcademicaController` valida que la sucursal tenga habilitada la modalidad de atención antes de crear/actualizar una oferta y el seeder `SucursalModalidadAtencionSeeder` precarga SPS/TGU con Presencial/Virtual. Falta verificación de migración/seeder en entorno real. |
| `P-054` | `completado` | Eliminar por completo la tabla y el modelo de reglas de aprobación. | Se borraron `app/Models/ReglaAprobacion.php`, `database/seeders/ReglaAprobacionSeeder.php` y la migración `2026_07_25_000082_create_reglas_aprobacion_table.php`; además se retiró `ReglaAprobacionSeeder` de `DatabaseSeeder`. No quedaron referencias de uso en validaciones ni pruebas. |
| `P-055` | `completado` | Crear una bitácora de auditoría central, exponerla en Seguridad y mostrarla en pantallas críticas. | Se añadió la migración `2026_08_24_000002_create_bitacora_auditoria_table.php`, el modelo `App\Models\BitacoraAuditoria` y nuevas capacidades en `ServicioBitacora` (`registrarAuditoria()` y `registrarAuditoriaDesdeRequest()`). `AuditoriaController` ahora expone `GET /api/v1/seguridad/auditoria/operaciones`, `GET /api/v1/seguridad/auditoria/entidad` y `GET /api/v1/seguridad/auditoria/operaciones/exportar` (CSV enriquecido con `valores_antes` y `valores_despues`). Se conectó el registro central a operaciones críticas de `Ofertas`, `Asistencias`, `Calificaciones`, `Gestiones de Matrícula`, `Pagos`, `Recibos`, `Usuarios` (crear, actualizar, asignar roles/sucursales y restablecer contraseña) y `Sesiones` (revocación manual). En UI, `resources/views/admin/seguridad.blade.php` muestra la nueva bitácora central dentro de la pestaña Auditoría con lectura más clara de `antes/después`, filtros más finos (`entidad_id`, `per_page`) y paginación visible; el modal de `Usuarios` muestra historial visible por entidad y también se añadieron bloques visibles de auditoría para `Roles`, `Permisos`, `Sucursales`, `Planes de cobro`, `Métodos de pago`, `Conceptos de pago`, `Períodos` y `Niveles`. Los modales críticos de `admin/matriculas`, `admin/pagos` y `admin/ofertas` consumen el endpoint por entidad para mostrar trazabilidad visible del registro actual. Verificado con `php artisan test tests/Feature/CalificacionTest.php --filter "registrar_calificaciones|registrar_asistencia_de_la_oferta|actualizar_calificacion"` (4 pruebas, 12 aserciones), `php artisan test tests/Feature/MatriculaTest.php --filter "reservar_matricula|confirmar_matricula|cancelar_matricula"` (6 pruebas, 28 aserciones) y `php artisan test tests/Feature/PagoTest.php --filter "registrar_pago_mediante_caso_de_uso|aprobar_pago_mediante_caso_de_uso_genera_recibo|rechazar_pago_mediante_caso_de_uso"` (3 pruebas, 16 aserciones). |
| `P-056` | `completado` | Permitir encender/apagar dinámicamente las bitácoras para reducir impacto en base de datos. | Se añadieron los servicios `ConfiguracionBitacorasService` y `RegistroBitacoraCorreoService`. `ServicioBitacora` ahora consulta parámetros globales antes de escribir `bitacora_auditoria`, `bitacora_peticiones` y `bitacora_seguridad`; el registro de `bitacora_correos` quedó centralizado y también respeta su propio toggle. `ParametroGlobalSeeder` ahora publica cuatro parámetros separados en el grupo `bitacoras`: `AUDITORIA_CENTRAL_HABILITADA`, `BITACORA_PETICIONES_HABILITADA`, `BITACORA_SEGURIDAD_HABILITADA` y `BITACORA_CORREOS_HABILITADA`. Con esto se puede apagar cada bitácora por separado desde Parámetros Globales, sin tocar `.env` ni reiniciar la aplicación, manteniendo fallback a la configuración existente cuando el parámetro aún no existe. También se sustituyeron los `BitacoraCorreo::create(...)` directos de `EstudianteAuthController` y `ProcesadorNotificacionAsistencia` por el nuevo registrador central. |
| `P-058` | `completado` | Implementar retención y purga automática por tipo de bitácora. | Se añadió `app/Console/Commands/PurgarBitacorasCommand.php` con soporte para `--dry-run` y override por opciones, y se programó ejecución diaria en `routes/console.php` (`bitacoras:purgar` a las `02:30`). `ConfiguracionBitacorasService` ahora resuelve retenciones separadas para `bitacora_auditoria`, `bitacora_peticiones`, `bitacora_seguridad` y `bitacora_correos`. `ParametroGlobalSeeder` publica los parámetros `RETENCION_AUDITORIA_DIAS`, `RETENCION_PETICIONES_DIAS`, `RETENCION_SEGURIDAD_DIAS` y `RETENCION_CORREOS_DIAS` en el grupo `bitacoras`, para ajustar la purga desde parámetros globales sin tocar código. |
| `P-057` | `completado` | Hacer configurable el TTL de caché de `ParametroGlobal`. | Se añadió `config/parametros_globales.php` con `PARAMETROS_GLOBALES_CACHE_TTL`. `ParametroGlobal::obtener()` ahora usa ese valor en segundos para la caché; si se configura `0`, deshabilita la caché y consulta directo a base. Esto aplica también a los toggles dinámicos de `bitacoras`, porque todos pasan por `ParametroGlobal::obtener()`. |
| `P-059` | `completado` | Simplificar el flujo de creación de enlaces de pago anticipados y habilitar su asignación automática por monto. | Se cerró el flujo completo de enlaces anticipados. `EnlacePago` ahora persiste `enlace_url`, `metodo_pago_id`, `estado_operativo`, `asignado_a_pago_id`, `asignado_a_estudiante_id`, `fecha_asignacion` y `fecha_uso`. `ResolverEnlacePagoDisponible` reserva solo enlaces `disponible` válidos del método correcto, incrementa usos y los amarra a pago/estudiante; además marca `usado` o `desuso` según el flujo. `PagoController` y `PortalEstudianteController` resuelven automáticamente un link disponible al solicitar pago por link, ponen el pago en `esperando_respuesta` y publican `link_pago_url`/`link_pago_estado`. `PortalEstudianteController@confirmarLinkPago` marca el enlace como `usado`; `reengancharFlujoPago` y `RechazarPago` lo pasan a `desuso`; `AprobarPago` consolida el enlace como `usado`. En `admin/pagos`, Contabilidad captura explícitamente la URL real del enlace, el método de pago y visualiza `estado_operativo`, manteniendo `Código` y `Nombre` como `readonly`. Verificado con `php artisan test tests/Feature/PagoTest.php --filter "crear_enlace_pago|listar_enlaces_pago|eliminar_enlace_pago|enlace_pago_disponible|registrar_pago_asigna_link_anticipado_disponible|aprobar_pago_marca_enlace_anticipado_como_usado|rechazar_pago_marca_enlace_anticipado_como_desuso|registrar_pago_solicita_link_y_pegar_link"` (8 pruebas, 29 aserciones) y `php artisan test tests/Feature/PortalEstudianteTest.php --filter "registrar_pago_link_sin_comprobante_queda_en_solicita_link|no_puede_solicitar_link_doble_para_misma_obligacion|registrar_pago_link_automatico_usa_enlace_valido|registrar_pago_link_automatico_rechaza_enlace_invalido|confirmar_link_pago_automatico_marca_enlace_usado|reenganchar_pago_link_automatico_marca_enlace_desuso"` (6 pruebas, 24 aserciones). |
| `P-052` | `en_progreso` | Mejorar la pantalla inicial `Portal Docente` de la APK para que sea más informativa y cambiar el menú principal a iconos con descripción. | Fase 1 y 2 implementadas en `mobile-docentes/`: `App.jsx` ya monta `src/components/DocenteHome.jsx` y `src/components/MainMenuGrid.jsx`; el home muestra datos del docente, estado de conexión/sincronización, tarjeta de perfil con nombre/correo/rol/sucursales y métricas rápidas. Además se refinó la UX interna de `Ofertas Académicas`, `Asistencia Diaria` y `Calificaciones`: tarjetas más claras, búsqueda local, resumen contextual, filtros de período mejor presentados, CTA principal consistente y edición guiada de la configuración WhatsApp del horario. El docente ahora puede modificar desde la APK tanto `whatsapp_grupo_nombre` como `whatsapp_link_periodo` usando el endpoint móvil `DocenteMovilController@actualizarWhatsappPeriodo`. También se añadió el módulo `Buscar Alumno`, que permite consultar si un estudiante está en alguna de las ofertas/horarios asignados al docente usando los datos sincronizados localmente, ver en cuántos horarios aparece, abrir directamente cada horario encontrado y mostrar indicadores por horario de asistencia cargada para hoy y calificación registrada. Versión subida a `0.1.14` y `versionCode 15`. Verificado con `./gradlew assembleRelease` en `mobile-docentes/android` y APK generada en `mobile-docentes/android/app/build/outputs/apk/release/app-release.apk`. Pendiente natural: extraer más subcomponentes de `App.jsx` y hacer aceptación visual/manual en dispositivo real. |
| `P-053` | `completado` | Guardar siempre usuario y fecha/hora de actualización en módulos académicos, operativos, seguridad y catálogos críticos. | Se reforzó backend para conservar auditoría explícita del usuario que registra/modifica y la marca temporal de la última actualización. `AsistenciaController` ya guardaba `registrado_por`; se añadió la migración `2026_08_24_000001_add_actualizado_en_to_asistencias_estudiante_table.php` y el modelo `AsistenciaEstudiante` ahora permite persistir `creado_en/actualizado_en`, de modo que `actualizado_en` ya no se pierda al actualizar. En `Calificaciones`, el modelo `Calificacion` ahora permite `creado_en/actualizado_en`; `RegistrarCalificaciones` y `ActualizarCalificacion` guardan `creado_por/actualizado_por` y timestamps explícitos; y `EloquentCalificacionRepositorio::crearOActualizar()` preserva los datos de creación originales cuando la fila ya existía y solo actualiza la auditoría de modificación. En `Matrículas`, el modelo `Matricula` ya admite `creado_en/actualizado_en` y los casos/repositorios ahora los setean explícitamente al reservar, confirmar o cancelar; además `GestionMatriculaController` quedó alineado en solicitar/aprobar/rechazar/ejecutar. En `Pagos`, `RegistrarPago`, `EloquentPagoRepositorio`, `ProveedorPagoController`, `EnlacePagoController`, `MetodoPagoController` y `ConceptoPagoController` ahora guardan `actualizado_por/actualizado_en` junto con creación, cambios de estado, links y configuración. En `Recibos`, `GeneradorReciboCaja` y `EloquentReciboCajaRepositorio` ahora dejan también auditoría explícita al emitir, reimprimir o anular. En catálogos académicos críticos, los modelos y controladores de `Ofertas`, `Planes`, `Versiones`, `Niveles`, `Horarios`, `Modalidades`, `Aulas`, `Períodos`, `Departamentos Académicos`, `Sucursales`, `Planes de cobro`, `Detalle de planes de cobro` e `Inventario` quedaron alineados para setear explícitamente `creado_por`, `actualizado_por`, `creado_en` y `actualizado_en` al crear, editar, desactivar o mover existencias. Finalmente, se cerró también una pasada del módulo `Seguridad`: modelos `User`, `Rol`, `Permiso`, `Modulo`, `OpcionModulo`, `ParametroGlobal` y `ConfiguracionFlujoMatricula` ya permiten `creado_en/actualizado_en`, y sus controladores principales ahora los setean explícitamente al crear, editar, desactivar, asignar roles/sucursales/permisos, restablecer contraseñas y actualizar configuraciones; `AuthController` ahora también deja explícito `creado_en` en sesiones e intentos y `actualizado_en` al bloquear usuarios por intentos fallidos. Verificado con `php artisan test tests/Feature/CalificacionTest.php` (22 pruebas, 69 aserciones), `php artisan test tests/Feature/MatriculaTest.php` (19 pruebas, 71 aserciones) y `php artisan test tests/Feature/PagoTest.php --filter "efectivo_requiere_monto_recibido_y_guarda_vuelto|aprobar_pago_mediante_caso_de_uso_genera_recibo|registrar_pago_mediante_caso_de_uso|rechazar_pago_mediante_caso_de_uso|actualizar_link_mediante_caso_de_uso"` (5 pruebas, 28 aserciones). La última pasada amplia de `Seguridad/Auth/Catálogo de pagos` se validó por revisión de código alineada al esquema; no se ejecutó una suite exclusiva adicional para esos controladores en este turno. |
| `P-036` | `completado` | Reconfigurar planes de estudio, niveles y ofertas demo según la nueva malla académica. | Se eliminó el histórico académico dependiente de la configuración anterior (`pagos`, `recibos`, `matrículas`, `obligaciones`, `gestiones`, `asistencias`, `calificaciones`, `historial`, certificados y ofertas), se dejó `CatalogoAcademicoSeeder` con los planes `Intensivo`, `Semi Intensivo`, `Infantil Intensivo` y `Infantil Semi Intensivo` con 36 niveles totales, y `OfertaAcademicaSeeder` ahora genera automáticamente ofertas base para todos los niveles en todas las sucursales del período activo. Estado actual verificado en base: `planes=4`, `niveles=36`, `ofertas=72`, `matriculas=0`, `pagos=0`; desglose por plan: `Intensivo=12`, `Semi Intensivo=24`, `Infantil Intensivo=12`, `Infantil Semi Intensivo=24`, y por sucursal `36` ofertas cada una. Evidencia: ejecución directa en base con `php artisan tinker`, validación de niveles/ofertas y `php artisan view:cache`. |
| `P-030` | `completado` | Evaluar si las consultas de lectura de Pagos (index, show, obligaciones-estudiante) deben pasar a repositorios de consulta. | **Decisión: no se extraen; quedan en el controlador como orquestación ligera.** `index` es listado paginado con eager loading, filtros y agrupado `clasificar` por estado (presentación del dashboard); `show` es lectura simple con relaciones; `obligacionesEstudiante` es consulta + mapeo de presentación cuya única regla de negocio (qué concepto habilita selección) ya vive en `ResolutorFlujoMatricula`. Ninguna cambia estados, requiere transacción ni dispara efectos secundarios. Aplicando el semáforo P-032 y la regla de arquitectura del 2026-08-10 ("dejar el CRUD simple y las lecturas en el controlador"), extraerlas solo trasladaría query-building a otra capa sin ganancia. La decisión aplica igual a las lecturas de Caja/Calificaciones/Inventario (P-035) ya delegadas a `index`/`show`/`kardex`. |
| `P-031` | `completado` | Convertir en `422` controlado la falta de existencia al vender libro en el registro de pago (hoy lanza `RuntimeException` que produce `500`). | `AplicadorEfectosPago::descontarLibroSiCorresponde` ahora lanza `App\Modules\Pagos\Exceptions\InventarioInsuficienteException` (dominio, extiende `DomainException`) y `RegistrarPago` la captura fuera de la transacción, lo que hace rollback del pago ya creado y devuelve `ResultadoCasoUso::error(422, 'No hay suficiente existencia. Disponible: X', '422_INVENTARIO_INSUFICIENTE')`; el controlador responde `422` con `codigo_error`. Verificado con `PagoTest` (30 pruebas, 120 aserciones, incluida `test_registrar_pago_con_libro_sin_existencia_devuelve_422_y_no_crea_pago` que además confirma que no se crea el pago, no se registra movimiento y la existencia no cambia) y suite completa (231 pruebas, 776 aserciones). |
| `P-029` | `completado` | Aplicar a Matrículas el patrón de casos de uso y repositorios de Pagos. | Extraídos a `app/Modules/Matriculas/`: `CasosUso/` `ReservarMatricula`, `ConfirmarMatricula` y `CancelarMatricula`; `Repositorios/` `MatriculaRepositorio`/`EloquentMatriculaRepositorio`; `Servicios/` `ValidadorPrerrequisitos`, `ValidadorConflictoHorario` y `GeneradorObligacionesMatricula`. La lógica de los traits `ValidaPrerrequisitos` y `ValidaConflictoHorario` se movió a servicios del módulo (los traits quedan solo para el portal estudiante). `ContextoUsuario` y `ResultadoCasoUso` se promovieron a `App\Modules\Comun` (compartido) y se actualizaron los 6 casos de uso de Pagos, `PagoController` y `PagoTest`. `MatriculaController` bajó de 336 a 166 líneas y solo orquesta HTTP. `MatriculaRepositorio` se vincula en `AppServiceProvider`. Verificado con `MatriculaTest` (16 pruebas, 5 nuevas de casos de uso), `PagoTest` (29) y suite completa (216 pruebas, 697 aserciones). |
| `P-035` | `completado` | Aplicar el patrón de casos de uso de Pagos/Matrículas a los módulos críticos restantes. | **Caja/Recibos completado:** extraídos a `app/Modules/Caja/` los casos de uso `AbrirSesionCaja`, `CerrarSesionCaja`, `AnularRecibo` y `ReimprimirRecibo` con sus repositorios (`SesionCajaRepositorio`, `ReciboCajaRepositorio`) y `Servicios/GeneradorDetallesCierre` (la agrupación por concepto/método que antes vivía en el controlador). `SesionCajaController` bajó de 191 a 125 líneas y `ReciboCajaController` delega reimprimir/anular. Los repositorios quedan vinculados en `AppServiceProvider`. Lecturas (`index`, `show`, `resumen`) se dejan en el controlador según P-030/P-032. Verificado con `CajaTest` (16 pruebas) y suite completa (224 pruebas, 723 aserciones). **Calificaciones completado:** extraídos a `app/Modules/Calificaciones/` los casos de uso `RegistrarCalificaciones` y `ActualizarCalificacion`; `Repositorios/` `CalificacionRepositorio`/`EloquentCalificacionRepositorio`; `Servicios/` `ValidadorAccesoOfertaDocente` (alcance docente que vivía en el controlador) y `SincronizadorHistorialAcademico` (el método privado que creaba/actualizaba `historiales_academicos`). `CalificacionController` bajó de 236 a 143 líneas y solo orquesta HTTP. El repositorio se vincula en `AppServiceProvider`. Los casos de uso reciben `docente_id` del usuario autenticado para el alcance de datos. Verificado con `CalificacionTest` (20 pruebas, 4 nuevas de casos de uso) y suite completa (228 pruebas, 739 aserciones). **Inventario completado:** extraídos a `app/Modules/Inventario/` los casos de uso `RegistrarInventario`, `AjustarExistencia` y `VenderLibro`; `Repositorios/` `InventarioRepositorio`/`EloquentInventarioRepositorio`; `Servicios/RegistradorMovimientoInventario` (crea los movimientos con `tipo_movimiento`, `existencia_antes/despues` y referencia al pago). `InventarioLibroController` bajó de 205 a 125 líneas y solo orquesta HTTP; `store`, `ajustar` y `vender` delegan en casos de uso y las lecturas (`index`, `show`, `kardex`) se mantienen en el controlador según P-030/P-032. `AjustarExistencia` y `VenderLibro` operan en transacción con bloqueo de fila, validan que la existencia no quede negativa y registran el movimiento; `VenderLibro` retorna `data.venta` con `inventario` y `total_venta` (contrato del test). El repositorio se vincula en `AppServiceProvider`. Verificado con `InventarioLibroTest` (24 pruebas, 81 aserciones; incluye pruebas directas de los casos de uso: `RegistrarInventario`, `AjustarExistencia` y `VenderLibro` con rechazos por duplicado, existencia negativa, stock insuficiente e inventario inexistente, y la regla de que un registro con existencia 0 no crea movimiento) y suite completa (241 pruebas, 813 aserciones). |
| `P-032` | `completado` | Definir una regla de decisión (semáforo) para saber cuándo extraer un caso de uso. | Formalizado en `docs/PATRON_MODULARIZACION_CASOS_USO.md`. **Extraer ahora** si cambia estados de negocio, toca más de una entidad, requiere transacción/bloqueo, dispara efectos secundarios, tiene validaciones de dominio repetidas o es difícil de probar desde el controlador. **Extraer después** si el módulo crecerá previsiblemente. **No extraer** si es CRUD directo, listado o lectura simple sin reglas relevantes (confirmado en `P-030` para lecturas de Pagos y aplicado a Caja/Calificaciones/Inventario). El documento incluye la estructura del módulo, las convenciones de `AppServiceProvider`/`app(CasoUso::class)` y los precedentes verificados de `P-028`/`P-029`/`P-035`. |
| `P-033` | `completado` | Decidir qué módulos modularizar y cuáles dejar como están, sin interrumpir el trabajo actual. | Formalizado en `docs/PATRON_MODULARIZACION_CASOS_USO.md`. **Modularizados:** `Pagos` (`P-028`), `Matriculas` (`P-029`), `Caja`/`Recibos`, `Calificaciones` e `Inventario` (`P-035`), porque concentran transiciones de estado, transacciones, efectos secundarios y validaciones de dominio reutilizables. **Se quedan como están:** catálogos simples, configuraciones CRUD sin transiciones relevantes, lecturas administrativas (`index`, `show`, `kardex`, `resumen`, consultas de alcance`) y reportes centrados en consulta/exportación. Los módulos CRUD/lectura que ganen reglas de negocio más adelante se reevalúan con el semáforo de `P-032`. |
| `P-034` | `completado` | Evitar que los repositorios se conviertan en "repositorios gordos" (mezclar persistencia con reglas de negocio). | Se creó `App\Modules\Pagos\Servicios\AplicadorEfectosPago` con las reglas de transición que vivían en `EloquentPagoRepositorio`: asignar sesión de caja, confirmar matrícula (con cupos), descontar libro/inventario, aplicar a obligaciones (pendientes y con selección), cancelar aplicaciones y revertir matrícula al rechazar. `PagoRepositorio`/`EloquentPagoRepositorio` quedan solo con persistencia y consultas (`buscar`, `buscarConBloqueo`, `crearPago`, `aprobar`, `actualizarLink`, `crearComprobante`, `marcarRechazado`, `eliminarDependenciasYRegistro`). `RegistrarPago`, `AprobarPago` y `RechazarPago` inyectan el servicio y delegan los efectos. Verificado con `PagoTest` (29 pruebas, 113 aserciones) y `MatriculaTest` (16). Suite completa: 220 passed, 4 failed en `CajaTest` por el trabajo en curso de `P-035` (INSERT de recibo sin `numero_recibo`), preexistente y ajeno a este cambio. |
| `P-011` | `completado` | Ocultar ofertas de periodos cerrados en el Portal del Estudiante. | `mis-ofertas` devuelve vacío sin periodo abierto; la reserva rechaza periodos cerrados con `422_PERIODO_NO_ABIERTO`. Verificado en `PortalEstudianteTest`. |
| `P-012` | `completado` | Mostrar en Matrícula Online el periodo académico en el que se está registrando el estudiante. | La API entrega código, nombre y fechas; la vista muestra la vigencia en el resumen, cada oferta y la confirmación. Verificado con `PortalEstudianteTest` y `npm run build`. |

### Higiene documental

| ID | Estado | Tarea | Evidencia o siguiente paso |
|---|---|---|---|
| `P-010` | `completado` | Depurar documentos históricos que todavía describen `backend/`, React o rutas antiguas. | Se marcaron explícitamente como históricos/obsoletos `docs/DESPLIEGUE_SMARTERASP.md`, `docs/ESTADO_IMPLEMENTACION_2026-07-23.md`, `docs/avance.md` y `docs/spec-feed-planCobro.md`. Además se corrigió `docs/PATRON_IMPLEMENTACION_RBAC.md` para usar rutas vigentes (`config/rbac.php`) y el patrón real del frontend actual (Blade + Alpine.js + `window.api`, no `frontend/src/rbac.jsx`). `DESPLIEGUE_SMARTERASP.md` ahora aclara que el proyecto es un monolito Laravel en la raíz con `public/` como directorio público y build desde la raíz con Vite. Verificado con búsqueda en `docs/` y revisión manual: las referencias residuales a `backend/`/`frontend/` quedan únicamente dentro de notas históricas o advertencias explícitas. |

### Notas operativas adicionales

- Operación de bitácoras: usar `docs/OPERACION_BITACORAS.md` como referencia rápida para toggles del grupo `bitacoras`, TTL de caché de parámetros globales, retención, purga manual y validación del scheduler.
- `P-021` (2026-08-10): `DistribucionApkDocenteTest` quedó ampliado a **9 pruebas / 42 aserciones**. La cobertura nueva valida descarga pública, registro desde servidor, selección del `.apk` más reciente cuando hay varias versiones colocadas y despublicación automática de la versión anterior al publicar una nueva. Estado local de la app: `mobile-docentes/app.json` está en `0.1.5` / `versionCode 6`; en `mobile-docentes/dist/` existen artefactos `v0.1.1`, `v0.1.2` y `v0.1.3`. Sigue pendiente la validación operativa/manual de qué versión está publicada efectivamente en la URL pública de producción y, si aplica, registrar/publicar la última desde panel o desde servidor.
- `P-021` (2026-08-22): se recompiló la APK firmada **v0.1.6 / versionCode 7** mediante `npx expo prebuild --platform android --no-install` y `gradlew.bat assembleRelease --no-daemon --console=plain`. Artefacto local: `mobile-docentes/android/app/build/outputs/apk/release/app-release.apk`, 66,625,374 bytes, SHA-256 `987C4F45D97F3EC8BA28BF5FB951A9EBB2FBCCB0AFD6DF02D118ACD770090045`. Se añadió `docs/PUBLICAR_APK_DOCENTES_TERMINAL.md` con el proceso de terminal seguro. Pendiente operativo: transferir este binario al directorio privado de producción por SFTP/FTPS/Plesk con una conexión autorizada y registrarlo mediante la API con un token Sanctum que tenga permisos de distribución; no hay credenciales ni token configurados en este entorno.
- `P-021` (2026-08-24): se incrementó la versión de la APK a **v0.1.8 / versionCode 9** y se generó el release firmado con `npx expo prebuild --platform android --no-install` y `gradlew.bat assembleRelease --no-daemon --console=plain`. Artefacto local: `mobile-docentes/android/app/build/outputs/apk/release/app-release.apk`, 69,053,630 bytes, SHA-256 `8DA6F42D1F804758B111A561B6858B35AD218A3C1C2E942D74D0A552109AD5E4`. `apksigner` verificó firma v2 con el certificado institucional. Pendiente operativo: transferir y publicar el APK mediante la conexión SFTP/FTPS/Plesk autorizada y el token Sanctum de distribución.
- `P-021` (2026-08-10): se implementó el backend móvil documentado en `docs/APK_DOCENTES_OFFLINE.md` con `GET /api/v1/docente-movil/sincronizar`, `POST /api/v1/docente-movil/sincronizar` y `GET /api/v1/docente-movil/ofertas/{id}`; además se añadió idempotencia por `uuid` en `sincronizaciones_docente_movil`. `DocenteMovilTest` cubre 4 pruebas / 18 aserciones: descarga solo de ofertas propias, detalle fuera de alcance, aplicación de asistencia/calificación offline y reintento sin duplicación. Verificado con suite completa: **320 pruebas, 1080 aserciones**.
- `P-021` (2026-08-10): `mobile-docentes/src/api.js` y `App.jsx` quedaron adaptados para consumir `docente-movil` en vez de combinar `/asistencias/*` y `/calificaciones/*`; la cola local ahora sincroniza una operación por alumno/asistencia y reenvía el `uuid` persistido para respetar la idempotencia del backend. `mobile-docentes/src/database.js` devuelve el `uuid` de cada operación local. Verificado con `expo lint` en `mobile-docentes/` y `php artisan test` (suite completa: **320 pruebas, 1080 aserciones**).
- `P-021` (2026-08-10): se añadió feedback visible de sincronización en `mobile-docentes/App.jsx`: el menú principal ahora muestra el último resultado de sincronización y hasta 3 pendientes con error, para no depender solo de `Alert`. Se evitó dejar cambios accidentales de ESLint/autoconfiguración en `mobile-docentes/`; el diff móvil quedó reducido a `App.jsx`, `src/api.js` y `src/database.js`. Verificado con `npx expo export --platform android` (bundle Android generado correctamente en `mobile-docentes/dist/`) y suite backend en verde.
- `P-021` (2026-08-10): la APK ahora incluye un módulo explícito **Sincronización** en el menú principal, con listado de operaciones pendientes, reintentos, errores/conflictos y acción de reenvío manual. Verificado con `npx expo export --platform android` tras el cambio (bundle Android correcto, `AppEntry-8741592b15fd00d25f47270a673f439d.hbc`).
- `P-021` (2026-08-10): la pantalla **Sincronización** ahora muestra además un resumen legible del payload pendiente por operación (fecha/matrícula/estado en asistencias; estudiante/nota/faltas en calificaciones), para facilitar soporte y resolución de conflictos sin inspeccionar JSON crudo. Verificado con `npx expo export --platform android` (bundle Android correcto, `AppEntry-e03483f64df1a7980f6cea9de3b3de07.hbc`).
- `P-021` (2026-08-10): la pantalla **Sincronización** permite ahora descartar manualmente una operación pendiente desde la APK cuando el docente decide no reenviarla. El descarte elimina el `uuid` de `cola_sincronizacion`, refresca el conteo local y deja un mensaje visible en el estado de sincronización. Verificado con `npx expo export --platform android` (bundle Android correcto, `AppEntry-5e7f0dc4b7ae453d66e6db3b0442aa11.hbc`).
- `P-021` (2026-08-24): el frontend web del docente quedó alineado para mostrar solo las vistas del docente en el menú administrativo cuando `sessionUser.docente_id` está presente. Se ocultaron módulos no docentes en `resources/views/layouts/admin.blade.php` y se conserva el acceso a `Mis Horarios`, `Asistencias`, `Calificaciones` y `APK Docentes`. Pendiente: validación visual/manual con usuario docente real y revisión de rutas directas si se desea bloqueo adicional.
- `P-052` (2026-08-24): nuevo frente para la APK docente. La petición vigente es mejorar `Portal Docente` para que la primera pantalla sea más informativa y el menú principal use iconos con descripción. Antes de implementarlo en paralelo, coordinar tres áreas separadas: `home/dashboard` docente, menú principal visual y ajustes de UX/navegación de módulos. Evitar tocar simultáneamente `mobile-docentes/App.jsx` sin una partición explícita por componentes.
- `P-021` (2026-08-25): corregida la cola offline de la APK docente: generaba identificadores con fecha/aleatorio que no cumplían el formato UUID requerido por `POST /api/v1/docente-movil/sincronizar`, por lo que el servidor rechazaba las calificaciones con `422`. La cola ahora genera UUID v4 válidos y conserva la operación si el servidor devuelve una operación no aplicada. Pendiente: compilación e instalación de la APK para validación manual en el teléfono.

## Criterio de cierre

Una tarea solo se marca `completado` cuando el cambio está implementado y
verificado. Una intención, una pantalla que solo oculta el problema o una
prueba manual no reproducible no bastan para cerrar una tarea crítica.
